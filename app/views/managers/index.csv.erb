<!-- Table downloaded as CSV when system manager archives data -->
<% headers = [
	'User Name',
	'Email'
] %>

<%= CSV.generate_line headers %>
<% @managers.each do |manager| %><%= CSV.generate_line([
  	manager.user_name,
  	manager.email
]) %><% end %>
