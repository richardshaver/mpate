<h1>Tasks</h1>

<p><%= link_to "Add a Task", new_task_path%></p>

<ul>
	<% @tasks.each do |task| %>
		<li><%= task.name %> - <%= link_to "Edit", edit_task_path(task) %>,  <%= link_to "Delete", task_path(task), method: :delete, confirm: "Are you sure?" %></li>
	<%end%>
</ul>
